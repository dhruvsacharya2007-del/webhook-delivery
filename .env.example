DATABASE_URL="postgresql://webhook:webhook@localhost:5432/webhook_db?schema=public"
PORT=3000
LOG_LEVEL=info
NODE_ENV=development
WEBHOOK_TIMEOUT_MS=10000

# Paste the signingSecret returned by POST /endpoints, then recreate the receiver:
#   docker compose up -d --force-recreate receiver
RECEIVER_SECRET=